import styled from "styled-components";

export const Container = styled.footer`
  max-width: 1120px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  height: 20vh;

  img {
    height: 115px;
  }

  a {
    color: #fff;
  }

  p {
    color: #fff;
  }

  strong {
    color: var(--text);
  }

  hr {
    margin: 0;
  }
`;
